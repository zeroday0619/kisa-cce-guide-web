---
criterionCode: U-08
title: 관리자 그룹에 최소한의 계정 포함
platforms: [rhel-10, ubuntu-26.04, debian-13]
status: final
sources:
  - title: "Ubuntu sudo-rs"
    url: https://ubuntu.com/server/docs/reference/other-tools/sudo-rs/
---

## 개요

### 점검 내용

시스템 관리자 그룹에 최소한(root 계정과 시스템 관리에 허용된 계정)의 계정만 존재 여부 점검

### 점검 목적

관리자 그룹에 최소한의 필요 계정만 존재하는지 확인하여 불필요한 권한 남용을 점검하기 위함

### 보안 위협

시스템을 관리하는 root 계정이 속한 그룹은 시스템 운영 파일에 대한 접근 권한이 부여되어 있으므로 해당 관리자 그룹에 속한 계정이 비인가자에게 유출될 경우, 관리자 권한으로 시스템에 접근하여 계정정보 유출, 환경설정 파일 및 디렉터리 변조 등의 위험이 존재함

## 점검 대상 및 판단 기준

### 대상

RHEL 10, Ubuntu 26.04 LTS, Debian 13

### 판단 기준

- **양호:** 관리자 그룹에 불필요한 계정이 등록되어 있지 않은 경우
- **취약:** 관리자 그룹에 불필요한 계정이 등록된 경우

### 조치 방법

관리자 그룹과 sudo 정책에 승인된 최소 계정만 유지

### 조치 시 영향

그룹에서 제거해도 기존 세션의 권한은 유지될 수 있으므로 권한 회수 시 활성 세션 종료 필요

## 점검 및 조치 사례

### RHEL 10

1. 관리자 그룹 및 대상 계정의 기본·보조 그룹 확인

   ```bash command
   getent group root wheel
   id USER
   sudo -l -U USER
   ```

2. `wheel` 및 root 그룹의 등록 사용자를 승인 대장과 대조. sudoers의 사용자 직접 부여 및 중앙 그룹 정책도 확인

3. 불필요한 보조 그룹 구성원 제거. 대상 그룹을 기본 그룹으로 사용하는 계정은 먼저 승인된 일반 그룹으로 기본 GID 변경

   ```bash command
   sudo gpasswd -d USER wheel
   sudo visudo -c
   ```

4. 기존 로그인 세션 종료 후 새 세션에서 `id` 및 `sudo -l` 결과 확인. 중앙 인증 그룹은 공급자의 권한 회수와 캐시 반영 상태 확인

### Ubuntu 26.04 LTS

1. 관리자 그룹 및 대상 계정의 기본·보조 그룹 확인

   ```bash command
   getent group root sudo
   id USER
   sudo -l -U USER
   ```

2. `sudo` 및 root 그룹의 등록 사용자를 승인 대장과 대조. sudoers의 사용자 직접 부여 및 중앙 그룹 정책도 확인

3. 불필요한 보조 그룹 구성원 제거. 대상 그룹을 기본 그룹으로 사용하는 계정은 먼저 승인된 일반 그룹으로 기본 GID 변경

   ```bash command
   sudo gpasswd -d USER sudo
   sudo visudo -c
   ```

4. 기존 로그인 세션 종료 후 새 세션에서 `id` 및 `sudo -l` 결과 확인. 중앙 인증 그룹은 공급자의 권한 회수와 캐시 반영 상태 확인

### Debian 13

1. 관리자 그룹 및 대상 계정의 기본·보조 그룹 확인

   ```bash command
   getent group root sudo
   id USER
   sudo -l -U USER
   ```

2. `sudo` 및 root 그룹의 등록 사용자를 승인 대장과 대조. sudoers의 사용자 직접 부여 및 중앙 그룹 정책도 확인

3. 불필요한 보조 그룹 구성원 제거. 대상 그룹을 기본 그룹으로 사용하는 계정은 먼저 승인된 일반 그룹으로 기본 GID 변경

   ```bash command
   sudo gpasswd -d USER sudo
   sudo visudo -c
   ```

4. 기존 로그인 세션 종료 후 새 세션에서 `id` 및 `sudo -l` 결과 확인. 중앙 인증 그룹은 공급자의 권한 회수와 캐시 반영 상태 확인
